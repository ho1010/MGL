# 🚀 시작하기 - 모바일 앱 배포

Management GL 앱을 모바일 앱으로 배포하는 가장 빠른 방법입니다.

## ⚡ 3단계로 시작하기

### 1단계: 환경 준비 (최초 1회만)

```powershell
# Android 개발 환경 자동 설정
.\scripts\setup-android-env.ps1
```

이 스크립트가:
- ✅ Android SDK 경로 찾기
- ✅ Java 경로 찾기
- ✅ 환경 변수 자동 설정

**또는 수동으로:**
1. Android Studio 설치: https://developer.android.com/studio
2. JDK 11 설치: https://adoptium.net/
3. 환경 변수 설정:
   - `ANDROID_HOME=C:\Users\Seol\AppData\Local\Android\Sdk`
   - `JAVA_HOME=C:\Program Files\Java\jdk-11`

### 2단계: 프로젝트 초기화

```powershell
# Android 네이티브 프로젝트 생성
.\scripts\init-android.ps1

# 의존성 설치
npm install
```

### 3단계: 빌드 및 테스트

```powershell
# Debug APK 빌드 (테스트용)
.\scripts\build-android.ps1 -BuildType debug

# APK를 기기에 설치
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

## 📦 Release 빌드 (배포용)

### 서명 키 생성

```powershell
.\scripts\generate-keystore.ps1
```

### Release 빌드

```powershell
# Release APK
.\scripts\build-android.ps1 -BuildType release

# AAB (Google Play Store용)
cd android
.\gradlew.bat bundleRelease
cd ..
```

## 📱 배포

빌드된 AAB 파일을 Google Play Console에 업로드하세요:
- `android\app\build\outputs\bundle\release\app-release.aab`

## 📚 자세한 가이드

- **빠른 가이드**: `빠른_배포_가이드.md`
- **전체 가이드**: `모바일_배포_완료_가이드.md`
- **상세 가이드**: `DEPLOYMENT_GUIDE.md`
- **체크리스트**: `배포_체크리스트.md`

## ❓ 문제 발생 시

1. 환경 변수 확인: `$env:ANDROID_HOME`
2. 새 PowerShell 창 열기
3. `scripts/README.md` 참조

**시작하세요!** 🎉

