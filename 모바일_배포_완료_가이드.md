# 📱 모바일 앱 배포 완료 가이드

이 가이드는 Management GL 앱을 모바일 앱으로 배포하는 전체 프로세스를 단계별로 안내합니다.

## 🎯 배포 프로세스 개요

```
1. 환경 설정 → 2. 프로젝트 초기화 → 3. 빌드 → 4. 테스트 → 5. 배포
```

## 🚀 빠른 시작 (5단계)

### 1단계: 환경 설정

```powershell
# Android 개발 환경 자동 설정
.\scripts\setup-android-env.ps1
```

또는 수동 설정:
- Android Studio 설치
- JDK 11 이상 설치
- 환경 변수 설정 (ANDROID_HOME, JAVA_HOME)

### 2단계: Android 프로젝트 초기화

```powershell
# Android 네이티브 프로젝트 생성
.\scripts\init-android.ps1
```

**참고:** 이미 `android` 폴더가 있으면 이 단계를 건너뛰세요.

### 3단계: 의존성 설치

```powershell
npm install
```

### 4단계: Debug APK 빌드 (테스트)

```powershell
# 테스트용 빌드
.\scripts\build-android.ps1 -BuildType debug
```

**빌드된 APK:**
```
android\app\build\outputs\apk\debug\app-debug.apk
```

### 5단계: APK 설치 및 테스트

**USB 연결:**
```powershell
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

**또는 직접 설치:**
- APK를 기기로 전송
- 파일 관리자에서 APK 열기
- 설치 허용

## 📦 Release 빌드 (배포용)

### 서명 키 생성

```powershell
.\scripts\generate-keystore.ps1
```

프롬프트에 따라:
1. 키 저장소 비밀번호 입력
2. 키 비밀번호 입력
3. 개인 정보 입력

**⚠️ 중요:** 키 파일과 비밀번호를 안전하게 보관하세요!

### Release APK 빌드

```powershell
.\scripts\build-android.ps1 -BuildType release
```

### AAB 빌드 (Google Play Store용)

```powershell
cd android
.\gradlew.bat bundleRelease
cd ..
```

**빌드된 AAB:**
```
android\app\build\outputs\bundle\release\app-release.aab
```

## 🏪 Google Play Store 배포

### 필수 준비물

1. **Google Play Console 계정**
   - https://play.google.com/console 접속
   - 개발자 등록 ($25 일회성)

2. **앱 정보**
   - 앱 이름: "혈당 관리"
   - 앱 설명 (한국어)
   - 스크린샷 (최소 2개)
   - 앱 아이콘 (512x512)
   - 개인정보처리방침 URL

3. **AAB 파일**
   - `app-release.aab` 준비

### 배포 단계

1. **앱 등록**
   - Google Play Console → 새 앱 만들기
   - 앱 이름 및 기본 언어 선택

2. **스토어 등록 정보 작성**
   - 앱 설명
   - 스크린샷 업로드
   - 아이콘 업로드
   - 기능 그래픽 업로드 (선택)
   - 개인정보처리방침 URL 입력

3. **콘텐츠 등급**
   - 설문 작성
   - 콘텐츠 등급 확인

4. **AAB 업로드**
   - 프로덕션 → 새 버전 만들기
   - AAB 파일 업로드
   - 출시 노트 작성

5. **심사 제출**
   - 검토를 위해 제출
   - 심사 완료까지 보통 1-3일 소요

## 📋 주요 스크립트 요약

| 작업 | 스크립트 | 설명 |
|------|---------|------|
| 환경 설정 | `.\scripts\setup-android-env.ps1` | Android 개발 환경 자동 설정 |
| 프로젝트 초기화 | `.\scripts\init-android.ps1` | Android 네이티브 프로젝트 생성 |
| Debug 빌드 | `.\scripts\build-android.ps1 -BuildType debug` | 테스트용 APK 빌드 |
| Release 빌드 | `.\scripts\build-android.ps1 -BuildType release` | 배포용 APK 빌드 |
| 서명 키 생성 | `.\scripts\generate-keystore.ps1` | 앱 서명 키 생성 |
| AAB 빌드 | `cd android && .\gradlew.bat bundleRelease` | Google Play용 AAB 빌드 |

## 📂 빌드 파일 위치

### APK 파일

- **Debug APK**: `android\app\build\outputs\apk\debug\app-debug.apk`
- **Release APK**: `android\app\build\outputs\apk\release\app-release.apk`

### AAB 파일

- **Release AAB**: `android\app\build\outputs\bundle\release\app-release.aab`

### 복사본

- 빌드 스크립트는 `builds\` 폴더에 타임스탬프가 있는 복사본도 생성합니다.

## 🔍 빌드 전 확인사항

### 필수 설정

- [ ] 앱 이름 설정 (`android/app/src/main/res/values/strings.xml`)
- [ ] 패키지 이름 설정 (`android/app/build.gradle`)
- [ ] 버전 번호 설정 (versionCode, versionName)
- [ ] 권한 설정 (AndroidManifest.xml)
- [ ] 앱 아이콘 설정

### 권한 확인

`android/app/src/main/AndroidManifest.xml`에 다음 권한이 포함되어 있는지 확인:

```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

## ⚠️ 주의사항

### 서명 키 관리

1. **안전한 보관**
   - 키 파일(`*.keystore`)을 안전한 곳에 백업
   - 키 비밀번호를 비밀 관리자에 저장

2. **버전 관리 제외**
   - `.gitignore`에 키 파일이 포함되어 있는지 확인
   - 키를 Git에 커밋하지 마세요!

3. **키 분실 시**
   - 키를 잃어버리면 앱 업데이트 불가능
   - 새 앱으로 등록해야 함

### 환경 변수

- `ANDROID_HOME`: Android SDK 경로
- `JAVA_HOME`: Java JDK 경로
- 환경 변수 설정 후 **새 PowerShell 창**을 열어야 적용됨

### 버전 관리

- Release 빌드 전에 버전 번호 증가
- `android/app/build.gradle`에서 `versionCode`와 `versionName` 수정

## 🐛 문제 해결

### 빌드 실패

```powershell
# Clean 후 재빌드
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

### Gradle 오류

```powershell
cd android
.\gradlew.bat --refresh-dependencies
```

### 환경 변수 오류

```powershell
# 현재 환경 변수 확인
$env:ANDROID_HOME
$env:JAVA_HOME

# 새 PowerShell 창에서 확인
```

### 메모리 부족

`android/gradle.properties`에 추가:
```properties
org.gradle.jvmargs=-Xmx4096m -XX:MaxPermSize=512m
```

## 📚 참고 문서

- **빠른 배포**: `빠른_배포_가이드.md`
- **전체 가이드**: `DEPLOYMENT_GUIDE.md`
- **빌드 가이드**: `BUILD_GUIDE.md`
- **아이콘 설정**: `APP_ICON_GUIDE.md`
- **체크리스트**: `배포_체크리스트.md`

## ✅ 다음 단계

1. **환경 설정 완료**
   ```powershell
   .\scripts\setup-android-env.ps1
   ```

2. **프로젝트 초기화**
   ```powershell
   .\scripts\init-android.ps1
   ```

3. **테스트 빌드**
   ```powershell
   npm install
   .\scripts\build-android.ps1 -BuildType debug
   ```

4. **앱 테스트**
   - APK를 기기에 설치
   - 모든 기능 테스트

5. **Release 빌드 및 배포**
   - 서명 키 생성
   - Release 빌드
   - AAB 빌드
   - Google Play Store 제출

## 🎉 완료!

모든 설정이 완료되었습니다. 이제 앱을 빌드하고 배포할 준비가 되었습니다!

**시작하기:**
1. `.\scripts\setup-android-env.ps1` 실행
2. `.\scripts\init-android.ps1` 실행
3. `빠른_배포_가이드.md`를 따라 진행하세요!

