# 🚀 빠른 배포 가이드

이 가이드는 Management GL 앱을 가장 빠르게 배포하는 방법을 안내합니다.

## ⚡ 5분 안에 빌드하기

### 1단계: 환경 준비 (최초 1회만)

```powershell
# 1. Node.js 설치 확인
node -v

# 2. Java 설치 확인
java -version

# 3. Android Studio 설치
# https://developer.android.com/studio 다운로드 및 설치

# 4. 환경 변수 설정 (시스템 설정에서)
ANDROID_HOME=C:\Users\Seol\AppData\Local\Android\Sdk
JAVA_HOME=C:\Program Files\Java\jdk-11
```

### 2단계: Android 프로젝트 초기화

```powershell
# Android 프로젝트 생성
.\scripts\init-android.ps1
```

**참고:** Android 폴더가 이미 있으면 이 단계를 건너뛰세요.

### 3단계: 의존성 설치

```powershell
# npm 패키지 설치
npm install
```

### 4단계: Debug APK 빌드 (테스트용)

```powershell
# Debug 빌드 (서명 불필요)
.\scripts\build-android.ps1 -BuildType debug
```

**빌드된 APK 위치:**
```
android\app\build\outputs\apk\debug\app-debug.apk
```

### 5단계: APK 설치 및 테스트

**USB 디버깅 활성화:**
1. 기기에서 설정 → 개발자 옵션 활성화
2. USB 디버깅 활성화
3. 컴퓨터에 USB 연결

**APK 설치:**
```powershell
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

**또는 직접 설치:**
- APK 파일을 기기로 전송 (USB, 이메일, 클라우드 등)
- 기기에서 파일 관리자로 APK 열기
- "알 수 없는 출처" 설치 허용

## 📦 Release 빌드 (배포용)

### 1단계: 서명 키 생성

```powershell
.\scripts\generate-keystore.ps1
```

프롬프트에 따라:
- 키 저장소 비밀번호 입력
- 키 비밀번호 입력
- 개인 정보 입력 (이름, 조직 등)

**⚠️ 중요:** 생성된 키 파일과 비밀번호를 안전하게 보관하세요!

### 2단계: Release APK 빌드

```powershell
.\scripts\build-android.ps1 -BuildType release
```

**빌드된 APK 위치:**
```
android\app\build\outputs\apk\release\app-release.apk
```

### 3단계: AAB 빌드 (Google Play Store용)

```powershell
cd android
.\gradlew.bat bundleRelease
cd ..
```

**빌드된 AAB 위치:**
```
android\app\build\outputs\bundle\release\app-release.aab
```

## 📱 Google Play Store 배포

### 간단 요약

1. **Google Play Console 계정 생성**
   - https://play.google.com/console 접속
   - 개발자 등록 ($25 일회성)

2. **앱 등록**
   - 새 앱 만들기
   - 앱 이름: "혈당 관리"
   - 기본 언어: 한국어

3. **AAB 업로드**
   - 프로덕션 → 새 버전 만들기
   - `app-release.aab` 업로드

4. **스토어 정보 작성**
   - 앱 설명
   - 스크린샷 (최소 2개)
   - 아이콘 (512x512)
   - 개인정보처리방침 URL

5. **심사 제출**

자세한 내용은 `DEPLOYMENT_GUIDE.md`를 참조하세요.

## 🎯 빌드 스크립트 요약

| 작업 | 명령어 |
|------|--------|
| Android 프로젝트 초기화 | `.\scripts\init-android.ps1` |
| Debug APK 빌드 | `.\scripts\build-android.ps1 -BuildType debug` |
| Release APK 빌드 | `.\scripts\build-android.ps1 -BuildType release` |
| 서명 키 생성 | `.\scripts\generate-keystore.ps1` |
| AAB 빌드 | `cd android && .\gradlew.bat bundleRelease` |

## 📂 빌드 파일 위치

| 파일 | 위치 |
|------|------|
| Debug APK | `android\app\build\outputs\apk\debug\app-debug.apk` |
| Release APK | `android\app\build\outputs\apk\release\app-release.apk` |
| AAB | `android\app\build\outputs\bundle\release\app-release.aab` |
| 서명 키 | `android\app\my-release-key.keystore` |

## ⚠️ 주의사항

1. **서명 키 보관**
   - 키 파일을 잃어버리면 앱 업데이트 불가
   - 키 파일을 버전 관리에 포함하지 마세요
   - 안전한 곳에 백업하세요

2. **환경 변수**
   - ANDROID_HOME과 JAVA_HOME이 올바르게 설정되어 있는지 확인

3. **버전 관리**
   - Release 빌드 전에 버전 번호를 증가시키세요
   - `android/app/build.gradle`에서 `versionCode`와 `versionName` 수정

## 🐛 문제 발생 시

### 빌드 실패
```powershell
# Clean 후 재빌드
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

### Gradle 오류
```powershell
cd android
.\gradlew.bat --refresh-dependencies
```

### 환경 변수 오류
```powershell
# 현재 설정 확인
$env:ANDROID_HOME
$env:JAVA_HOME
```

## 📞 도움말

더 자세한 내용은:
- `DEPLOYMENT_GUIDE.md` - 전체 배포 가이드
- `BUILD_GUIDE.md` - 상세 빌드 가이드
- `MOBILE_APP_SETUP.md` - 모바일 앱 설정

