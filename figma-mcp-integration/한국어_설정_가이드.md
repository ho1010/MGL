# Figma MCP 통합 프로젝트 - 설정 가이드

## ✅ 완료된 작업

1. ✅ 프로젝트 구조 생성 (`figma-mcp-integration/` 폴더)
2. ✅ 모든 설정 파일 생성:
   - `package.json` - 프로젝트 의존성 및 스크립트
   - `tsconfig.json` - TypeScript 설정
   - `tsup.config.ts` - 빌드 설정
   - `Dockerfile` - Docker 설정
   - `smithery.yaml` - MCP 설정
   - `.gitignore` - Git 무시 규칙
3. ✅ WebSocket 서버 파일 생성 (`src/socket.ts`)
4. ✅ 하위 디렉토리 설정 파일 생성:
   - `src/talk_to_figma_mcp/package.json` ✅
   - `src/talk_to_figma_mcp/tsconfig.json` ✅
   - `src/cursor_mcp_plugin/manifest.json` ✅
   - `scripts/setup.sh` ✅
5. ✅ 문서 생성:
   - `README.md` - 전체 문서
   - `QUICK_START.md` - 빠른 시작 가이드
   - `SETUP_STATUS.md` - 현재 상태
   - `FINAL_COPY_INSTRUCTIONS.md` - 복사 안내
   - `한국어_설정_가이드.md` - 이 문서

## ⚠️ 필요: 큰 소스 파일 복사

다음 큰 파일들을 수동으로 복사해야 합니다:

### 복사할 파일들:

1. **MCP 서버 파일** (~3100줄):
   - `d:\101 program\cursor-talk-to-figma-mcp-main\cursor-talk-to-figma-mcp-main\src\talk_to_figma_mcp\server.ts`
   - → `figma-mcp-integration\src\talk_to_figma_mcp\server.ts`

2. **Figma 플러그인 파일** (~4000줄):
   - `d:\101 program\cursor-talk-to-figma-mcp-main\cursor-talk-to-figma-mcp-main\src\cursor_mcp_plugin\code.js`
   - → `figma-mcp-integration\src\cursor_mcp_plugin\code.js`
   
3. **기타 플러그인 파일들**:
   - `src\cursor_mcp_plugin\ui.html`
   - `src\cursor_mcp_plugin\setcharacters.js`

## 📋 복사 방법

### 방법 1: Windows 탐색기 사용 (추천)

1. Windows 탐색기를 엽니다
2. 다음 경로로 이동:
   ```
   d:\101 program\cursor-talk-to-figma-mcp-main\cursor-talk-to-figma-mcp-main\src
   ```
3. `talk_to_figma_mcp` 폴더 전체를 복사하여 다음 경로에 붙여넣기:
   ```
   C:\Users\Seol\Documents\GitHub\MGL\figma-mcp-integration\src\
   ```
4. `cursor_mcp_plugin` 폴더 전체를 복사하여 다음 경로에 붙여넣기:
   ```
   C:\Users\Seol\Documents\GitHub\MGL\figma-mcp-integration\src\
   ```

### 방법 2: PowerShell 명령어 사용

PowerShell을 관리자 권한으로 실행하고 다음 명령 실행:

```powershell
$source = "d:\101 program\cursor-talk-to-figma-mcp-main\cursor-talk-to-figma-mcp-main"
$dest = "C:\Users\Seol\Documents\GitHub\MGL\figma-mcp-integration"

# MCP 서버 복사
Copy-Item -Path "$source\src\talk_to_figma_mcp" -Destination "$dest\src\" -Recurse -Force

# Figma 플러그인 복사
Copy-Item -Path "$source\src\cursor_mcp_plugin" -Destination "$dest\src\" -Recurse -Force

# 스크립트 복사
Copy-Item -Path "$source\scripts" -Destination "$dest\" -Recurse -Force

Write-Host "파일 복사 완료!" -ForegroundColor Green
```

## ✅ 복사 후 확인

다음 파일들이 있는지 확인하세요:

- [ ] `figma-mcp-integration/src/talk_to_figma_mcp/server.ts`
- [ ] `figma-mcp-integration/src/cursor_mcp_plugin/code.js`
- [ ] `figma-mcp-integration/src/cursor_mcp_plugin/ui.html`
- [ ] `figma-mcp-integration/src/cursor_mcp_plugin/setcharacters.js`

## 🚀 다음 단계

파일 복사가 완료되면:

### 1. 의존성 설치

```powershell
cd figma-mcp-integration
bun install
```

### 2. 프로젝트 빌드

```powershell
bun run build
```

### 3. Cursor MCP 설정

`~/.cursor/mcp.json` 파일을 생성하거나 편집:

```json
{
  "mcpServers": {
    "TalkToFigma": {
      "command": "bun",
      "args": ["C:/Users/Seol/Documents/GitHub/MGL/figma-mcp-integration/src/talk_to_figma_mcp/server.ts"]
    }
  }
}
```

### 4. WebSocket 서버 시작

```powershell
cd figma-mcp-integration
bun socket
```

이 터미널은 계속 실행 상태로 유지하세요!

### 5. Figma 플러그인 설치

1. Figma Desktop 앱을 엽니다
2. **Plugins** > **Development** > **New Plugin...** 클릭
3. **Link existing plugin** 선택
4. 다음 파일 선택:
   ```
   C:\Users\Seol\Documents\GitHub\MGL\figma-mcp-integration\src\cursor_mcp_plugin\manifest.json
   ```
5. 플러그인이 개발 플러그인 목록에 나타납니다
6. 플러그인을 실행합니다

### 6. 연결 및 사용!

1. 플러그인 UI에서 "Connect" 버튼 클릭
2. Cursor에서 채널에 연결: `join_channel` 도구 사용
3. Cursor AI로 Figma와 상호작용 시작!

## 📁 현재 프로젝트 구조

```
figma-mcp-integration/
├── src/
│   ├── socket.ts ✅
│   ├── talk_to_figma_mcp/
│   │   ├── package.json ✅
│   │   ├── tsconfig.json ✅
│   │   └── server.ts ❌ (복사 필요)
│   └── cursor_mcp_plugin/
│       ├── manifest.json ✅
│       ├── code.js ❌ (복사 필요)
│       ├── ui.html ❌ (복사 필요)
│       └── setcharacters.js ❌ (복사 필요)
├── scripts/
│   └── setup.sh ✅
├── package.json ✅
├── tsconfig.json ✅
├── tsup.config.ts ✅
└── (기타 설정 파일들) ✅
```

## 🔍 문제 해결

### 파일이 복사되지 않는 경우

1. 소스 경로가 올바른지 확인:
   ```
   d:\101 program\cursor-talk-to-figma-mcp-main\cursor-talk-to-figma-mcp-main\src
   ```
2. 대상 경로가 올바른지 확인:
   ```
   C:\Users\Seol\Documents\GitHub\MGL\figma-mcp-integration\src
   ```
3. 관리자 권한으로 PowerShell 실행 후 다시 시도

### Bun이 설치되지 않은 경우

```powershell
powershell -c "irm bun.sh/install.ps1|iex"
```

### MCP가 작동하지 않는 경우

1. `~/.cursor/mcp.json` 경로 확인
2. 파일 경로가 절대 경로인지 확인
3. WebSocket 서버가 실행 중인지 확인 (`bun socket`)
4. Figma 플러그인이 연결되어 있는지 확인

## 📚 추가 참고 자료

- `README.md` - 전체 프로젝트 문서
- `QUICK_START.md` - 빠른 시작 가이드
- `SETUP_STATUS.md` - 현재 상태 및 진행 상황

## 요약

✅ 모든 설정 파일과 구조가 준비되었습니다!
⏳ 큰 소스 파일들(server.ts, code.js)만 복사하면 됩니다
🚀 복사 후 `bun install` 및 나머지 설정을 진행하세요

